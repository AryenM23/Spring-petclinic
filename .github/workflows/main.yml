name: Reflect E2E Tests

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  reflect-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Stap 1: Installeer Node.js (nodig voor de Reflect CLI)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Stap 2: Installeer en run Reflect
      - name: Run Reflect Tests
        env:
          REFLECT_API_KEY: ${{ secrets.REFLECT_API_KEY }}
        run: |
          # 1. Installeer de juiste CLI tool (deze bestaat wel)
          npm install -g @reflect-run/cli
          
          # 2. Run de specifieke suite uit je screenshot
          # We gebruiken hier de naam "Addowner" zoals in je dashboard staat
          reflect --apiKey $REFLECT_API_KEY --suite "Addowner"
